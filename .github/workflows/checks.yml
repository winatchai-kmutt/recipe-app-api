---
# ubuntu-20.04 => It already has Docker installed
# docker/login-action@v1 => Login to Docker Hub for increased pull base images limits
# actions/checkout@v2 => Ensure the code is available before running
# Use docker-compose with Docker Compose
name: Checks
on: [push]

jobs:
  test-lint:
    nanme: Test and Lint
    run-on: ubuntu-20.04
    steps:
      - name: Login to Docker Hub
        user: docker/login-action@v1
        with:
          usernames: ${{ secrets.DOCKERHUB_USER }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - name: Checkout
        user: actions/checkout@v2
      - name: Test
        run: docker-compose run --rm app sh -c "python mamage.py test"
      - name: Lint
        run: docker-compose run --rm app sh -c "flake8"